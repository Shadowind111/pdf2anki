# Example Input Note

This folder intentionally does not include medical PDFs or generated Anki decks.

To test the app:

1. Place a legally usable PDF outside the repository or in a local ignored folder.
2. Run `py .\pdf2anki.py`.
3. Configure a small page range.
4. Review the generated `.apkg` in Anki.
